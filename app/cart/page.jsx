import { CartView } from "@/components/cart-view";

export const metadata = {
  title: "Cart",
  description: "Review your Meraki Artencial Store cart before checkout.",
};

export default function CartPage() { return <CartView />; }
